export type DateString = string;

export enum ProjectName {
    ll = 'll',
    lls = 'lls',
    lln = 'lln',
    llss = 'llss',
}

export enum Category {
    chara = 'chara',
    seiyuu = 'seiyuu',
    couple = 'couple',
}
