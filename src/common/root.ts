export type DateString = string;

export enum ProjectName {
    ll = 'll',
    lls = 'lls',
    lln = 'lln',
    llss = 'llss',
    rest = 'rest'
}

export enum Category {
    chara = 'chara',
    person = 'person',
    couple = 'couple',
}
